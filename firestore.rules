rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true; // herkes okuyabilir
      allow write: if request.auth != null && request.auth.uid == 'SENIN_UID_N'; // sadece sen yazabilirsin
    }
  }
}